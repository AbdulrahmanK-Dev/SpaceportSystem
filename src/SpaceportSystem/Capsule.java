package SpaceportSystem;

public class Capsule {

    
    
   // E.StartEngine();
  
   // E.ChangeThrottleSetting(80);
    
    //E.CalculateCurrentThrust();
    
    
    //f.setCurrentFuel(400,E);
    
    
    
   // f.consumeFuel(f.getFuelBurnRate(E), 50);
    
    
/*    Passenger P = new Passenger("Ghazy","241014797","2007","Maadi",true);
    Ticket T= new Ticket("8z8z",1000000.00,BookingStatus.CONFIRMED,"2");
    Passenger P2 = new Passenger("Akari Watanabe","241002312","2008","Japan",true);
    BookingManager B = new BookingManager();
     
    B.Book(P, T);
    
   B.ChangeBook(P2, P, T);
    
    System.out.println(P.getPassengerTicket());
  
 
     
    
  /*
    long lastUpdate = System.nanoTime();

    while(engine.isOperational()) {
        long now = System.nanoTime();
        double deltaTime = (now - lastUpdate) / 1_000_000_000.0;
        lastUpdate = now;

        double burnRate = tank.computeBurnRate(engine);
        boolean ok = tank.consumeFuel(burnRate, deltaTime);

        if(!ok) {
            engine.ShutdownEngine();
            break;
        }

        Thread.sleep(50); // optional pause
    }
*/
    
}
